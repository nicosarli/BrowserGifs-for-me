import { createTheme } from '@material-ui/core/styles';

const theme = createTheme ({
  palette: {
    action: {
      disabled: '#fff'
    },
    background: {
      paper: '#f99',
      default: '#f99'
    }
  }
})

export default theme